create database student;


create table usename(
        id int  primary key auto_increment,
        username varchar(20),
        password varchar(40),
        nikename varchar(20),
        createDate Timestamp

);